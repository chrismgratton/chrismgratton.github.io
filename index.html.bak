<!DOCTYPE html>
<html lang="EN">

<head>
    <title>Styled Button Example</title>
<style>
    body {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        background-color: #184d47; /* Dark Green Background */
        color: #f0e9e2; /* Off-white text color for better readability on dark background */
    }
    .feedback-container {
        background-color: #d4ac0d; /* Golden background for the container */
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .feedback-container input[type="text"],
    .feedback-container input[type="email"],
    .feedback-container select,
    .feedback-container textarea {
        width: 100%;
        padding: 10px;
        margin: 10px 0;
        border: 1px solid #c0392b; /* Deep red border */
        border-radius: 4px;
        background-color: #f0e9e2; /* Off-white background for input fields */
        color: #000; /* Black text in input fields */
    }
    .feedback-container button {
        padding: 10px 20px;
        background-color: #27ae60; /* Green button */
        border: none;
        color: #fff; /* White text on the button */
        font-size: 16px;
        border-radius: 5px;
        cursor: pointer;
    }
</style>
    <title>solitaire</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="index.css">
</head>

<body>

    <div id="overlay">
        <span id="instructionText">Press Start to begin</span>
        <button id="startButton" class="PrimaryButton">Start</button>
    </div>

    <div class="ParentContainer">
        <button id="newButton" class="PrimaryButton" onclick="location.reload()">New Game</button>
        <button id="pauseButton" class="PrimaryButton StartPauseTimer">||</button>
        <span id="timer" class="PrimaryButton StartPauseTimer">00:00</span>
        <span id="moveCounter" class="PrimaryButton StartPauseTimer">Moves: 0</span>
		<button id="helpButton" class="PrimaryButton" onclick="window.location.href='feedback.html'">Help</button>
    </div>
    
    <canvas id="GameBoard" width="1000" height="1000"></canvas>
    <script src="shell.script.js" type="module"></script>
    <script src="Timer.js"></script>
</body>

</html>
